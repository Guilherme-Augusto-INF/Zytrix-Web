{
  "name": "zytrix-web",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "python3 scripts/build-policies.py",
    "test": "node --test tests/report-model.test.mjs",
    "check": "python3 scripts/check-site.py",
    "test:rules": "firebase emulators:exec --project demo-zytrix-governance --only firestore 'node --test tests/firestore.rules.test.mjs'"
  },
  "dependencies": {
    "firebase-admin": "14.3.0",
    "stripe": "22.6.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "firebase": "^12.0.0",
    "firebase-tools": "^14.0.0"
  }
}
